# Neon Postgres pooled connection string (used by app queries)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Neon direct connection string (used by Prisma schema operations)
DIRECT_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Fallback ingestion token for all projects (unless overridden per project)
INGEST_API_TOKEN="replace-with-a-strong-token"

# Default project partition when payload omits project_key
DEFAULT_PROJECT_KEY="inkyframe"

# Optional per-project token overrides as JSON map
# Example: {"inkyframe":"token-a","wanikami":"token-b"}
PROJECT_TOKENS_JSON="{}"

# Optional stale threshold for UI status checks
STALE_AFTER_SECONDS="600"

# Daily telemetry email summaries via Resend (same provider pattern as wazadb)
RESEND_API_KEY="re_replace_with_resend_api_key"
EMAIL_FROM="SumiLabu Telemetry <noreply@sumilabu.com>"
TELEMETRY_SUMMARY_RECIPIENTS="you@example.com"
CRON_SECRET="replace-with-a-strong-cron-secret"
APP_BASE_URL="https://app.sumilabu.com"
SUMMARY_UTC_OFFSET_HOURS="-8"
