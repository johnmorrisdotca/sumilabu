# Neon Postgres pooled connection string (used by app queries)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Neon direct connection string (used by Prisma schema operations)
DIRECT_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Fallback ingestion token for all projects (unless overridden per project)
INGEST_API_TOKEN="replace-with-a-strong-token"

# Default product/project partition when payload omits project_key
DEFAULT_PROJECT_KEY="inkyframe"

# Optional per-project token overrides as JSON map.
# Use product keys such as inkyframe for SumiLabu Clock and onibako for Onibako.
# Do not commit real secrets.
# Example: {"inkyframe":"token-a","onibako":"token-b"}
PROJECT_TOKENS_JSON="{}"

# Optional UI health thresholds, used only for a device that reports no
# heartbeat_interval_s. One that reports it is judged by its own interval
# (offline after three missed), never sooner than STALE_AFTER_SECONDS.
EXPECTED_HEARTBEAT_SECONDS="1800"
STALE_AFTER_SECONDS="5400"

# Board, settings and reports: three separate token maps, same
# { "<projectKey>": "<token>" } shape, kept apart so a leaked key in one
# never grants another (docs/board/BOARD_RULES.md, docs/board/REPORTS_CONTRACT.md).
# BOARD_TOKENS_JSON moves tickets - every agent worktree holds one.
# SETTINGS_TOKENS_JSON reads/writes settings - only a site's production
# deployment holds one.
# REPORTS_TOKENS_JSON creates/lists/reads/patches/deletes reports, and
# authorizes GET /api/v1/health; it does NOT authorize filing a report
# (that needs BOARD_TOKENS_JSON).
# Do not commit real secrets.
BOARD_TOKENS_JSON="{}"
SETTINGS_TOKENS_JSON="{}"
REPORTS_TOKENS_JSON="{}"
